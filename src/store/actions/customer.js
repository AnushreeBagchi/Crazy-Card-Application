import { getAvailableCardsAction } from "./actions";
import { createAction} from "@reduxjs/toolkit";

export const getAvailableCards = state => {
   return state
}


export const addCustomer = createAction("addCustomer");

